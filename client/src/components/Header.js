import React, {Component} from 'react';
import {MenuTarget} from '../teleporters/Menu'

export default class Header extends Component {
    render(){
        return (
         
          <MenuTarget />
 
        )
    }
}